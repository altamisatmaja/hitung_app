part of 'component.dart';

class IpoComponent extends StatelessWidget {
  const IpoComponent({super.key});

  @override
  Widget build(BuildContext context) => const Scaffold(
        body: Center(
          child: Text('IPO Components'),
        ),
      );
}